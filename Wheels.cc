#include<stdio.h>
#include"Cell.h"
#include"Data.h"
#include"Algorithm.h"
#include"Sensors.h"
#include"Motor.h"


using namespace std;
    
    void stop();  //stop the mouse from moving
    //these four functions move each wheel of the mouse so it can turn and face the right direction
    //int get_velocity();
    //void pulse(bool forward);
    double set_velocity(int timer, int distance);
    //void move_pulses(int amount);
    
void pulse(bool forward)
{
    
}


int get_velocity()
{
    
    
}

void move_pulses(int amount)
{
    
}